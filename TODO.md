# TODOs del proyecto

Inventario de los comentarios `TODO` que siguen vivos en la capa mock de API.

## API mock

| Archivo | Línea | Descripción |
| --- | ---: | --- |
| `src/api/auth.ts` | 35 | Reemplazar el mock de `login` por una llamada real al cliente API. |
| `src/api/auth.ts` | 44 | Reemplazar el mock de `register` por una llamada real al cliente API. |
| `src/api/auth.ts` | 58 | Reemplazar el mock de `logout` por una llamada real al cliente API. |
| `src/api/auth.ts` | 64 | Reemplazar el mock de `getMe` por una llamada real al cliente API. |
| `src/api/cart.ts` | 14 | Reemplazar el mock de `estimateCart` por una llamada real al cliente API. |
| `src/api/content.ts` | 17 | Reemplazar el mock de `getContentBlocks` por una llamada real al cliente API. |
| `src/api/orders.ts` | 83 | Reemplazar el mock de `getOrders` por una llamada real al cliente API. |
| `src/api/orders.ts` | 89 | Reemplazar el mock de `getOrderByReference` por una llamada real al cliente API. |
| `src/api/orders.ts` | 96 | Reemplazar el mock de `initiateCheckout` por una llamada real al cliente API. |
| `src/api/products.ts` | 71 | Reemplazar el mock de `getProducts` por una llamada real al cliente API. |
| `src/api/products.ts` | 89 | Reemplazar el mock de `getProductBySlug` por una llamada real al cliente API. |
| `src/api/products.ts` | 95 | Reemplazar el mock de `getRelated` por una llamada real al cliente API. |
| `src/api/wishlist.ts` | 6 | Reemplazar el mock de `getWishlist` por una llamada real al cliente API. |
| `src/api/wishlist.ts` | 12 | Reemplazar el mock de `addToWishlist` por una llamada real al cliente API. |
| `src/api/wishlist.ts` | 18 | Reemplazar el mock de `removeFromWishlist` por una llamada real al cliente API. |

## Nota

Estos `TODO` son intencionales: marcan exactamente qué funciones de la capa `src/api/` deben conectarse al backend real cuando la API esté disponible.
