import { PrismaClient } from '@prisma/client'; import { orderService } from './src/services/orderService'; const prisma = new PrismaClient(); async function main() { try { const o = await orderService.createOrderFromCart('cmrcd0am30000gopra3n64ja4', 'test@test.com', '123', 'Test', {addressLine1:'1',city:'2',department:'3'}); console.log('Order:', o); } catch (e) { console.error(e); } } main().finally(() => prisma.$disconnect());
